<div class="row"> 
  <div class="col-md-4">
<%= form_for(requete) do |f| %>
  <% if requete.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(requete.errors.count, "error") %> prohibited this requete from being saved:</h2>

      <ul>
      <% requete.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :nom,:class => "form-control input-sm" %>
  </div>

  <div class="actions">
    <%= f.submit 'Ajouter', :class => "btn btn-sm btn-primary" %>
  </div>
<% end %>
</div>
</div>